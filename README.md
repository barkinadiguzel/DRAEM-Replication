# DRAEM-Replication
DRAEM-Replication implements the DRÆM surface anomaly detection pipeline with reconstructive and discriminative sub-networks. The model produces pixel-level anomaly maps and image-level anomaly scores based on joint reconstruction and discriminative embedding.
